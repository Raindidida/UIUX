services:
  - type: web
    name: idiom-deathmatch-server
    runtime: node
    rootDir: game-server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
